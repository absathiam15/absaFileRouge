api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
    collection: 
        pagination: 
            items_per_page: 5
    eager_loading:
        force_eager: false
    formats:
        json:   ['application/json']
        jsonld: ['application/ld+json']
        jsonhal:    ['application/hal+json']
        jsonapi:    ['application/api+json']